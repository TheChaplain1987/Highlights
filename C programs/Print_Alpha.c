// Program to print the alphabet

#include <stdio.h>

int main( void ) {
    char c;

    // Print aplhabet
    for ( c = 'a'; c <= 'z'; c++ ) {
        printf("%c ", c);
    }
    printf("\n");

    // Exit program
    return 0;
}
