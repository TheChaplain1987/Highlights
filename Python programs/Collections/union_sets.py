# Program to get the union of two sets

# Make two sets
developers = { 'Mark', 'George', 'Ben' }
designers = { 'Andy', 'Jared', 'Ben' }

# Use union() to find distinct elements from both sets
result = developers.union( designers )

# Print the union
print( result )
