# Program to print in date format

# User inputs data
day = input('Enter day (xx):')
month = input("Enter month (xx): ")
year = input('Enter year (xxx): ')

# Print is date format
print('Date format: ', month + '/' + day + '/' + year )
